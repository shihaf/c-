#include <iostream>
#include <string>
#include<cstring>
using namespace std;

int main() {
	string s,a;char temp;
    cin>>s>>a;
    cout<<s.size()<<" "<<a.size();
    cout<<"\n"<<s<<a<<"\n";
    temp=a[0];
    a[0]=s[0];
    s[0]=temp;
    cout<<s<<" "<<a;
  
    return 0;
}

